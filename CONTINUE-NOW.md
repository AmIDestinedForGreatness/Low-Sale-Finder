# Continue prompt — paste this to keep it working

Keep going. Re-read `AGENT-RELAY.md` from the bottom up, starting with your
own last entry ("real footer parser defect closed without accepting the
16-record premise," commit `801885b`) — that entry already names the next
unit: acquire one original named source (M Blastoise-EX 22/108, M
Manectric-EX 024a/119, Mimikyu V 068/172, or a binder page), preserve
source hash/raw OCR/crop provenance the same way you did for Mime Jr., and
reproduce its current pipeline failure before touching OCR logic.

Same governing rules as before — they haven't changed, don't re-derive them
from scratch: `CODEX-IDENTIFICATION-NEXT-PROMPT.md` in the repo root is
still the active spec (adversarial confusion-pair testing before accepting
any recall improvement, no threshold loosening ever, real provenance only —
no manufacturing ground truth from your own prediction or from listing
titles, separate OCR-stage success from final-identification success,
checkpoint every unit into `AGENT-RELAY.md` in the same format you already
used).

Check machine state fresh — don't assume yesterday's finding still holds.
If `fingerprints.sqlite`/real catalog images are now available, HASH-FIRST
real-photo acceptance (Priority 2 in the spec) becomes viable too; if not,
stay on footer-OCR/identification-parity work.

One live update since your last entry: the previous 45-record failure
premise you corrected (15 unique footer-human cases, not 16) is the current
canonical count — don't re-litigate that, it's settled. Sign your next
relay entry with a short agent label (e.g. `CG |`) so a three-agent relay
(Claude Code / Codex / you) stays easy to scan for who did what.

Keep working through additional units — don't stop after one. Same hard
boundaries as always: no push, no deployment, no marketplace/seller
actions, no starting Track B/Kino/marketplace expansion, no re-opening the
already-closed `app.py` security surface unless you find a genuinely new
defect there. Continue until you hit a real wall or the session ends, then
leave a full handoff the way your last entry did (that one was a good
template — matched it).
