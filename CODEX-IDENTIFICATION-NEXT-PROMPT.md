# CODEX CONTINUATION PROMPT — PRIORITIZE IDENTIFICATION ACCURACY, NOT MORE GENERAL HARDENING

You are continuing work on Yujin's Pokestop / Low-Sale-Finder, a Pokémon TCG
card-identification and deal-finding system.

The previous Codex session completed valuable security and reliability work:
fixing duplicated dashboard identification logic that missed the `m20`
garbage-name rejection, correcting a supposedly-passing HASH-FIRST test that
was silently skipping, dashboard authentication, URL/SSRF validation, upload
size/type restrictions, failure-log write locking, and blocking silent
real-network calls in the test suite. **Do not repeat or redesign those
completed units unless current tests reveal a regression.**

The next session must focus on the core product question: **can the system
identify difficult real Pokémon cards more accurately without becoming more
confidently wrong?**

## 0. The claimed evidence — VERIFY, DO NOT TRUST

Claude Code's own review of `FAILURES.md` produced these claims. **Treat them
as claims to verify directly from the current repository, not as established
fact**, before relying on them for prioritization:

- 16 of 45 recorded identification failures (~35%) reportedly share the exact
  cause "OCR did not read the footer; a human eye reading the same footer
  succeeded." Named cases claimed: Meloetta XY120, Victini XY117, Manaphy
  XY113, Yveltal-EX XY150, Magearna XY186, M Manectric-EX 024a/119,
  Incineroar-GX 27/149, Weavile 238/236, Wishiwashi 240/236, Stoutland
  248/236, Excadrill 246/236, Mimikyu V 068/172, Coalossal 117/100 (x2 — two
  separate records), M Blastoise-EX 22/108, Rota's Mime Jr. 086/PCG-P. That
  is 16 names for 16 claimed records — cross-check the count matches before
  treating it as ground truth.
- ~10 records reportedly cite missing artwork evidence, supporting future
  HASH-FIRST work, but the required fingerprint database may not be
  available on this machine.
- **Machine-state note, confirmed tonight on this checkout (Mom's PC):**
  `fingerprints.sqlite` does not exist here. `uploads/` exists but currently
  contains only synthetic fixtures generated by `tests.py` runs (gitignored,
  tiny generated test images — not real card photos). Re-check this yourself
  rather than assuming it's still true if you're running on a different
  machine or after further test runs; if it's still true here, Priority 1
  (footer OCR, which doesn't need the fingerprint DB) is where real work is
  actually possible on this machine, and Priority 2 is likely blocked again.

## 1. NON-NEGOTIABLE REPOSITORY ORIENTATION

Before modifying code, read: `README.md`, `DIRECTIVE.md`, `VISION.md` in
full, `PROGRESS.md`, `AGENT-RELAY.md` (newest entries first), `LESSONS.md`,
`FAILURES.md`, `HASH-FIRST-NEXT.md`, `docs/CLAUDE-CODE-AGENT-EVALUATION.md`,
and any newer Markdown handoff.

Then run `git status`, `git diff --stat`, `git diff`, `git log --oneline -20`.
Determine: which previous changes are committed vs. uncommitted, whether the
working tree has unrelated owner/agent work, whether `main` is ahead of
`origin/main`, whether the full suite is currently green, and whether
`fingerprints.sqlite`/`uploads/`/real-photo assets are actually available
(don't trust section 0's note blindly either — confirm it yourself).

Preserve all valid existing work. **Do not push to GitHub.**

## 2. PRIMARY OBJECTIVE

Investigate and improve collector-number/footer OCR recovery using the
repository's real recorded failures. Do not begin with a broad OCR rewrite.
Do not add a new OCR framework merely because it appears architecturally
cleaner.

First prove: which recorded failures are actually caused by footer OCR (not
just claimed to be), which pipeline stage loses the readable number, whether
the number is absent from OCR entirely, misread, cropped incorrectly,
normalized incorrectly, or discarded later; which smallest intervention
improves the real cases; whether that intervention creates new false-positive
identifications.

Loop: **inspect → quantify → reproduce → baseline → failing test → smallest
correction → adversarial validation → exact metrics → documentation → local
commit → reassess.** Do not stop after writing an analysis.

## 3. VERIFY THE FAILURE DATA

Parse or manually audit `FAILURES.md` and any corresponding structured
failure logs. Build an evidence table (failure ID, source image/fixture,
expected card/set/number/language, whether the footer is visible to a human,
raw OCR output, normalized OCR output, crop coordinates, candidate produced,
final failure category, artwork availability, whether listing metadata
influenced the result, reproducibility on this machine) — **start with a
representative sample of 5-6 of the claimed 16 footer records to establish
the pattern is real, then expand to the rest incrementally as you build
fixtures, rather than blocking all code work until every record is fully
tabulated.** The goal is verified evidence before a fix, not an exhaustive
audit before any fix.

Do not silently count duplicate records as separate failures. Distinguish:
footer not present in the image; footer present but cropped out; crop present
but too low-resolution; OCR returned no text; OCR returned corrupted text;
correct text existed but normalization destroyed it; correct text existed but
downstream matching ignored it; candidate snapping changed the value; exact
number read but exact printing still remained ambiguous.

Record verified totals in `AGENT-RELAY.md`. Use numbers, not "many," "most,"
or "significant."

## 4. BUILD A REPRODUCIBLE FOOTER-OCR CORPUS

Use legitimate existing repository images and fixtures. Create or extend a
deterministic test corpus for the verified footer failures. Each fixture must
preserve: source provenance, expected card identity, expected raw and
normalized collector-number text, expected language, whether exact set
identification should be possible, whether the system should identify or
abstain, and why the fixture is legally/operationally acceptable to retain.

**Do not manufacture ground truth from the system's own prediction. Do not
use listing titles as ground truth.** Where the original image is
unavailable, document the record as non-reproducible rather than fabricating
a replacement.

Synthetic transformations may supplement real fixtures but must be labeled
separately: real photo / existing dataset image / cached marketplace image /
synthetic blur / synthetic perspective distortion / synthetic crop /
synthetic glare / synthetic compression / synthetic rotation. **Never report
synthetic degradation results as real-photo results.**

## 5. ESTABLISH AN HONEST BASELINE

Before implementing a correction, run the current pipeline against the
reproducible footer corpus. Record at minimum: total reproducible cases,
footer crop successfully located, raw number correctly/partially/incorrectly
read or not returned at all, correct normalized number, correct/incorrect
final card, correct/incorrect abstention, wrong high-confidence result,
runtime per image, external OCR calls attempted, cases skipped and the exact
reason for every skip.

A skipped test is not a pass. A test that didn't exercise the intended OCR
path is not evidence. Add assertions proving every benchmark fixture actually
reaches the intended footer/OCR stage.

## 6. TRACE THE COMPLETE FOOTER PATH

Map image → card geometry detection → perspective correction → footer crop
selection → crop resolution/scaling → contrast/threshold preprocessing → OCR
provider invocation → raw OCR preservation → character normalization →
collector-number extraction → candidate generation → catalog filtering →
edit-distance/number snapping → contradiction checking → confidence
calculation → final identification or abstention. Identify where each real
failure occurs.

Temporary diagnostic tooling is fine but must not leave sensitive, noisy, or
unbounded debug output enabled by default. Useful diagnostics: saved footer
crop, crop dimensions, preprocessing strategy used, OCR text by strategy,
candidate numbers generated, edit distances, catalog candidates before/after
filtering, reason a candidate was rejected, reason the system abstained or
committed. Do not overwrite original images.

## 7. INVESTIGATE SMALL, EVIDENCE-BACKED CORRECTIONS

Evaluate interventions one at a time — none pre-approved just because they're
listed here: footer localization/geometry-relative cropping/multiple narrow
footer regions/skew guards/minimum-crop-resolution checks; preprocessing
(resolution-aware upscaling, grayscale, local contrast enhancement, adaptive
thresholding, controlled sharpening, inversion where justified, multiple
variants); OCR aggregation (a small bounded set of footer-specific passes,
preserving every raw reading, combining only independently meaningful
readings, avoiding confidence inflation from correlated variants); number
parsing (support formats already demonstrated by repository evidence — XY120,
024a/119, 123/198, promo prefixes, lowercase suffixes, leading zeros, JP
formats already in the catalog — do not introduce speculative formats without
evidence).

**Candidate validation:** use catalog constraints to evaluate OCR candidates,
but do not silently rewrite OCR into the nearest catalog number. Always
preserve raw OCR, normalized OCR, proposed correction, edit distance,
candidate source, catalog constraint, and whether another direct signal
confirms the correction. A catalog-nearest candidate is an inference, not
direct OCR evidence.

**Abstention:** when the number remains ambiguous, valid output is "number
unresolved," "variant unresolved," "several candidates remain," "insufficient
footer evidence," "exact printing not proven." **Do not lower confidence or
matching thresholds merely to produce more identifications.**

## 8. TEST-FIRST REQUIREMENT

For every correction: add a regression test representing a confirmed real
failure → confirm it fails before the change, for the expected reason →
implement the smallest robust correction → run the targeted test, then
related OCR/identification tests, then the full suite (`python tests.py`,
exact pass/fail/skip/error counts) → `LESSONS.md` entry → `AGENT-RELAY.md`
checkpoint → focused local commit → no push.

Do not weaken an existing test to make the new implementation pass. Do not
replace exact assertions with broad truthiness assertions. Do not hide
external dependencies behind automatic skips.

## 9. REQUIRED ADVERSARIAL TESTING — this is the section that matters most

A footer-OCR improvement is unacceptable if it merely increases recall by
introducing dangerous false matches. Challenge every proposed fix with:
another card whose number differs by one character; leading-zero differences;
letter/digit confusion (O/0, I or l/1, S/5, B/8, Z/2); promo-prefix
ambiguity; lowercase-suffix ambiguity; partial denominator; missing slash;
cropped prefix/suffix; multiple numbers visible in frame; attack damage
mistaken for collector number; HP mistaken for collector number; copyright
year mistaken for collector number; graded-label number mistaken for card
number; seller watermark digits; multiple cards in one image; listing
metadata contradicting the visible footer; correct artwork but wrong
printing; same artwork reused across sets; a catalog candidate existing at a
close edit distance.

Measure both additional correct identifications AND additional incorrect
identifications. **A change that recovers five cards but causes one confident
wrong exact-printing match is unacceptable under `DIRECTIVE.md`** — that
trade is a net loss, not a net win, no matter how good the recall number
looks in isolation.

## 10. SEPARATE OCR SUCCESS FROM IDENTIFICATION SUCCESS

Report independently: footer extraction performance (correct/partial/
incorrect/no raw reading, correct normalized reading); candidate-resolution
performance (correct candidate present/ranked first, incorrect candidate
ranked first, tie, none); final identification performance (correct exact
printing, correct card but unresolved printing, correct/incorrect abstention,
incorrect identification, incorrect HIGH-CONFIDENCE identification).

Do not call OCR fixed merely because the final card was correct through
artwork or metadata compensating. Do not call identification fixed merely
because OCR produced the expected substring on one fixture.

## 11. ACCEPTANCE CRITERIA FOR THE FIRST FOOTER-OCR UNIT

Complete only if: the footer-failure count is verified from repository
evidence (not assumed from section 0); at least one real reproducible
failure has a deterministic failing regression test; the failure is traced
to a specific pipeline stage; a bounded correction is implemented; the
regression test passes afterward; adversarial tests (section 9) remain safe;
no new incorrect high-confidence result is introduced in the evaluated
corpus; exact before/after metrics are recorded; `python tests.py` remains
green; `LESSONS.md` gets the closed lesson; `AGENT-RELAY.md` gets a complete
checkpoint (template in section 15); a focused local commit is created; no
push occurs.

After the first unit, continue to the next independently reproducible
footer failure or shared root cause. Do not stop merely because one example
passes.

## 12. HASH-FIRST / ARTWORK WORK

Remains high-value — ~10 records reportedly lack artwork corroboration
(verify this count too, per section 0). But do not claim progress on
real-photo HASH-FIRST verification unless this machine actually contains
`fingerprints.sqlite`, the required catalog fingerprints, real permitted
card photographs, expected ground truth, and the relevant upload/dataset
directories.

**If the assets exist:** follow `HASH-FIRST-NEXT.md`, verify every test
executes rather than skips, run the real-photo acceptance test, report
correct/incorrect/abstained/failed/skipped separately, preserve false-match
analysis, never lower the hash threshold merely to increase acceptance.

**If the assets do not exist** (per section 0's machine-state note, likely
true on this checkout): record the exact missing paths, record which tests
cannot run, do not fabricate replacements, **continue footer-OCR work
instead**, leave a precise Personal-PC handoff. The absence of HASH-FIRST
assets is not permission to pivot into unrelated roadmap work — go back to
Priority 1, not to something else entirely.

## 13. SCOPE BOUNDARIES

Do not begin: Track B inventory automation, marketplace expansion, seller
posting, autonomous buyer messaging, Kino, condition grading, broad pricing
architecture, mobile app development, multi-TCG expansion, a complete
OCR-engine rewrite, a large canonical-service rewrite without a reproduced
inconsistency, or **new security work in `app.py`'s auth/SSRF/upload code
unless a NEW identification-blocking defect surfaces there** — that surface
was closed last session; don't re-open it just because it feels productive.
Security regressions must still get fixed if found, but this session's
primary metric is identification quality, and the owner explicitly wants to
see the `FAILURES.md` count go down on named real cards, not another
infrastructure-hardening pass.

## 14. AGENT BEHAVIOR REQUIREMENTS

Act as an independent engineering reviewer, not as an agent trying to
validate previous claims (including this prompt's own section-0 numbers).
Verify counts yourself. Preserve raw evidence. Report uncertainty.
Distinguish observed behavior from inference. Correct your own methodology
when a test isn't exercising what it claims. Reject silent skips and
accidental live-network tests. Avoid broad architectural churn. Add
permanent tests for confirmed failures. Prefer honest abstention over forced
matching. Record numbers instead of adjectives. Keep commits focused. Leave
the repository understandable to the next agent, whichever one it is.

Do not describe something as "fixed," "robust," "production-ready,"
"complete," "fully verified," or "accurate" unless the documented evidence
supports that exact scope. Use language like: "Recovered 3 of 8 reproducible
footer cases," "No new incorrect matches in the 14-case adversarial set,"
"Live Google Vision behavior not tested," "Number recovery improved, but
exact variant remains unresolved," "Result depends on catalog inference
rather than direct OCR," "Four historical records could not be reproduced
because source images were unavailable."

## 15. REQUIRED CHECKPOINT AFTER EACH UNIT

Append to `AGENT-RELAY.md`: **Unit** (exact problem investigated) —
**Evidence** (failure records/fixtures used) — **Baseline** (exact
before-change results) — **Root cause** (specific stage responsible) —
**Regression test** (name, why it failed before the fix) —
**Implementation** (files/functions changed) — **Verification** (every
command run, exact result) — **Real-data status** (real photo / dataset
image / cached marketplace image / synthetic transformation, per record) —
**Safety result** (incorrect matches, abstentions, confidence changes,
adversarial outcomes) — **Limitations** (what remains unresolved) —
**Commit** (local hash, or exact reason it failed) — **Next unit** (the next
highest-value reproducible identification weakness).

## 16. FINAL SESSION HANDOFF

Before ending, record: current branch, `git status`, local commits ahead of
remote, files changed, verified footer-failure count (vs. section 0's
claimed 16), reproducible vs. unavailable cases, before/after OCR metrics,
before/after exact-identification metrics, incorrect high-confidence
results, new regression tests, full-suite result, external OCR/network calls
made, HASH-FIRST asset availability, uncommitted changes, remaining
assumptions, the single best next identification unit, and the part of your
own implementation the next agent should challenge first.

Do not push to GitHub. Do not end with "everything is complete" — this
project remains in development.

**Begin now: verify section 0's claimed counts against the actual
repository, confirm this machine's asset availability, then produce a
baseline on 5-6 real reproducible footer cases before modifying any
production code.**
